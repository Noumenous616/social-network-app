import React from 'react';
import preloader from '../../accets/preloader.gif';

let Preloader = (props) => {
    return <div>
            <img src={preloader} />
        </div>

}

export default Preloader;
